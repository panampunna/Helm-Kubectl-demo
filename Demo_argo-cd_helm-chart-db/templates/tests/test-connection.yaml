apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "Demo_argo-cd_helm-chart-db.fullname" . }}-test-connection"
  labels:
    {{- include "Demo_argo-cd_helm-chart-db.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "Demo_argo-cd_helm-chart-db.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
