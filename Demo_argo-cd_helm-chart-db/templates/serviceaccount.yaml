{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "Demo_argo-cd_helm-chart-db.serviceAccountName" . }}
  labels:
    {{- include "Demo_argo-cd_helm-chart-db.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
{{- end }}
