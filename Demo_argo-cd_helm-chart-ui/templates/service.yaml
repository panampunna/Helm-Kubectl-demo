apiVersion: v1
kind: Service
metadata:
  name: {{ include "Demo_argo-cd_helm-chart-ui.fullname" . }}
  labels:
    {{- include "Demo_argo-cd_helm-chart-ui.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "Demo_argo-cd_helm-chart-ui.selectorLabels" . | nindent 4 }}
